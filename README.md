# azure-vagrant
Project for deploying Dockerised apps to Azure using Vagrant

Prerequisites:

Setup management keys and certificates in the Azure portal
Setup private Docker registry in Azure
Setup credentials for Docker registry in Azure
Push Docker images to Docker registry in Azure
Define local environment variables for DOCKER_USER, DOCKER_PASSWORD and DOCKER_REGISTRY
Run Vagrant up 
